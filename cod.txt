def f(x:Int,y:Char):Int
begin
    Let x:Int;
    Let x:Int;
endDef;

Class abc
begin
    Let x:Int;
endClass;


def main():Int
begin

    Let x:Array<PINT>[10];
    x.x.x.x=2;
    x=f.exit(1,2,x,y.z,t(),t.z());

    eval((x+y+z)*2+2/2-1);

    Let x:Int;
    if(False)
    begin
        Let x:Int;
    end

    for(x=1;False;x=2)
    begin
        Let x:Int;
    end

    x=y.f(1,2,3);

endDef;


